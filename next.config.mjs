/** @type {import('next').NextConfig} */
const nextConfig = {
  // Strict mode for better React practices
  reactStrictMode: true,
  
  // Hide dev indicator (Next.js logo in bottom corner)
  devIndicators: false,
};

export default nextConfig;
